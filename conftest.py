# Presence of this file makes pytest add the project root to sys.path (prepend
# import mode), so tests can `from lib ...`, `from workflow ...`, and
# `from scripts ...` without an installed package. No app code lives here.
