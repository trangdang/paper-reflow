[project]
name = "paper-reflow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.28.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.14.0",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
